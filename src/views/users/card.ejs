<!DOCTYPE html>
<div class="card">
  <h1 class="whatever"><%= user.name%></h1>
  <img src="<%= user.image%>" alt="" style="width:200px; height:200px;">
  <h1><%= user.current_city%></h1>
  <h1><%= user.created_at.toString().slice(0, 15) %></h1>
</div>